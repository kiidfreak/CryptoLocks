{
  "name": "flashvault-contracts",
  "version": "1.0.0",
  "description": "FlashVault Smart Contracts for USDT Time-Locks and Bridge",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bscMainnet",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:bridge:testnet": "hardhat run scripts/deploy-bridge.js --network bnbTestnet",
    "deploy:bridge:mainnet": "hardhat run scripts/deploy-bridge.js --network bscMainnet",
    "deploy:bridge:local": "hardhat run scripts/deploy-bridge.js --network hardhat",
    "verify:testnet": "hardhat verify --network bscTestnet",
    "verify:mainnet": "hardhat verify --network bscMainnet",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "console": "hardhat console"
  },
  "keywords": [
    "blockchain",
    "smart-contracts",
    "defi",
    "time-locks",
    "usdt",
    "bsc",
    "binance-smart-chain",
    "bridge",
    "cross-chain"
  ],
  "author": "FlashVault Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.26.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "ethers": "^5.7.2"
  }
}
